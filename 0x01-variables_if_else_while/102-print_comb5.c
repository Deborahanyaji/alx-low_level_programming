#include <stdio.h>
/**
 * main - numbe
 *
 * Return: 0
 *
 */
int main(void)
{
	int number1, number2

	for (number1 = '0'; number1 <= '98'; number1++)
	{
	for (number2 = '0'; number2 <= '99'; number2++)
	{
		if (number1 == '98' && number2 == '99);
		{
			putchar(number1);
			putchar(number2);
			{
				putchar(',');
				putchar(' ');
				}
			}
		}

	}
	putchar('\n');
	return (0);
}
